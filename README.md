# Currency-Converter-Chatbot
A simple Python chatbot that converts currencies in real time using an exchange rate API.   The project demonstrates how to fetch live data from an API and interact with a user through a command-line chatbot.
